# Clippy configuration for ai-trade-bot
# https://doc.rust-lang.org/clippy/configuration.html

# Allow unwrap() only in tests
allow-unwrap-in-tests = true

# Threshold for "large" enum (default 200 bytes)
large-error-threshold = 200
